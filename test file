first line

added -enfluid in ~/.ssh/config file for host name
removed indiviual user.name from project .git/config file


restored user.name in project .git/config file


cloned with insolz attacher

updated config file with name and email